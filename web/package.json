{
  "name": "propwash-web",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.4"
  }
}
